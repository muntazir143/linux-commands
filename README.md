# Linux Commands

Present Working Directory
```bash
pwd
```

Listing Files
```bash
ls
```

Change Directory
```bash
cd
cd/folder-name
```

IF ELSE
```bash
a=2
b=4

if [ $a -gt $b ]
then
  echo "a is greater than b"
else
  echo "b is greater than a"
fi
```
